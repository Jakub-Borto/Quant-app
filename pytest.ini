# Pytest configuration for the repo.
# legacy_streamlit/ is a frozen, intentionally-broken reference copy of the old
# Streamlit frontend — never collect tests (or import anything) from it.
[pytest]
norecursedirs = legacy_streamlit venv data .git orderbook_replay_rs
