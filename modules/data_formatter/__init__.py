"""
modules.data_formatter — the Data Formatter module (raw DBN -> enriched parquet).

backend/   pure folder scanning extracted verbatim from
           legacy_streamlit/views/data_formatter.py. The transforms themselves
           are drop-in plugins in the configured data_transforms folders.
window.py  the PySide6 frontend (added in the rebuild).
"""
